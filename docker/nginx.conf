# 天工 Nginx 反向代理（容器内，固定 IP 172.28.0.10，与 docker-compose.yml 的 TIANGONG_TRUSTED_PROXIES 对应）
server {
    listen 80;
    server_name _;

    # 执行附件上限 200 MB，留余量
    client_max_body_size 210m;

    location / {
        proxy_pass http://app:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # AI 分析/拆解/生成为后台任务，页面轮询进度；附件解析上传与导出仍可能较慢
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
        proxy_request_buffering off;
    }
}

# HTTPS 示例：准备好证书后取消注释，并在 docker-compose.yml 打开 443 端口与 certs 挂载
# server {
#     listen 443 ssl;
#     server_name tiangong.example.com;
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#     client_max_body_size 210m;
#     location / {
#         proxy_pass http://app:8000;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 300s;
#         proxy_send_timeout 300s;
#         proxy_request_buffering off;
#     }
# }
